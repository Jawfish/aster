id: prefer-strenum
valid:
  - |
    class Status(StrEnum):
        ACTIVE = auto()
  - |
    class Color(Enum):
        RED = 1
        GREEN = 2
  - |
    class MyClass(Base, Mixin):
        pass
invalid:
  - |
    class Status(str, Enum):
        ACTIVE = "ACTIVE"
  - |
    class Status(Enum, str):
        ACTIVE = "ACTIVE"
